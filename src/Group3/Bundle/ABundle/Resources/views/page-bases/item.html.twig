{% extends "Group3ABundle::layout.html.twig" %}

{% block list %}
    {{ listHeader.itemHeader() }}

    {% for item in items %}
        {{ listItem.item(item, active)}}
    {% endfor %}

{% endblock %}