{% extends "Group3ABundle::layout.html.twig" %}

{% block list %}
    {{ listHeader.orderHeader(startDate, stopDate) }}

    {% for order in orders %}
        {{ listItem.order(order, active)}}
    {% endfor %}

{% endblock %}